## Linked issue
<!-- Closes #<n>. If this PR is part of the KMP migration, link the phase issue. -->

## What changed
<!-- Plain-language summary. Note any user-visible behavior changes, API surface changes, or build-config shifts. -->

## How to validate locally
<!-- The commands a reviewer can run to reproduce the result. Examples:
  - `./gradlew check assembleDebug :showkase-processor:test`
  - `./gradlew :sample:installDebug` then launch the Showkase browser
  - For multi-target work later: `./gradlew :showkase:iosSimulatorArm64Test` (macOS only)
-->

## CI status
<!-- Confirm the CI workflow is green on this branch before requesting review. -->

## Out-of-scope follow-ups
<!-- Anything noticed during this work that should become a separate issue.
     Keep this PR focused; cut new tickets rather than expanding scope. -->
